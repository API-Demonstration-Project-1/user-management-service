spring:
  application:
    name: user-management-service
  profiles:
    active:
      default
  cloud:
    config:
      uri: https://localhost:8888
logging:
 level:
  com.toystore.ecomm: DEBUG